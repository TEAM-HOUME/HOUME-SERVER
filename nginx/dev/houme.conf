# dev.houme.kr — 서버의 /etc/nginx/sites-available/houme.conf 로 배치
# prod 와 구조 100% 동일. 차이는 server_name·cert 경로·green 포트뿐이다.
# dev 는 단일 컨테이너(8080)라 blue/green 이 같은 곳을 가리킨다 (전환해도 무해).

upstream blue  { server 127.0.0.1:8080; }
upstream green { server 127.0.0.1:8080; }

server {
    listen 80;
    listen [::]:80;
    server_name dev.houme.kr;

    # certbot 갱신/발급용
    location /.well-known/acme-challenge/ {
        root /var/www/html;
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name dev.houme.kr;

    ssl_certificate     /etc/letsencrypt/live/dev.houme.kr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dev.houme.kr/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # blue/green 전환 상태 — CD 가 이 파일을 덮어쓰고 reload 한다
    include /etc/nginx/service-env.inc;

    location / {
        proxy_pass http://$service_url;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";

        proxy_redirect off;
        proxy_connect_timeout 180s;
        proxy_send_timeout 180s;
        proxy_read_timeout 180s;
    }
}
